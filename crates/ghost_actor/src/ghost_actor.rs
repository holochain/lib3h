//use crate::*;

#[cfg(test)]
mod tests {
    //use super::*;

    mod proto {
        /*
        ghost_protocol! {
            prefix(test),
            event_to_actor(print, String),
            request_to_actor(add_1, i32, Result<i32, ()>),
        }
        */
    }

    #[test]
    fn it_should_run_actor() {
    }
}
